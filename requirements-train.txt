-r requirements.txt

# --- Downscaler + SIREN training (Option B default trainable core) ---
# Deliberately no flash-attn here: it needs torch present in its own
# isolated build environment to compile (pip's build isolation means
# "torch is already installed" in your venv doesn't help it), it requires
# a CUDA toolchain to build at all, and nothing in this codebase actually
# uses it yet -- the model's attention layers (app/models/layers.py) use
# plain nn.MultiheadAttention. It's a real future optimization for GPU
# training, not a dependency of anything that runs today.
pytorch-lightning==2.4.0
hydra-core==1.3.2
omegaconf==2.3.0
einops==0.8.0
mlflow==2.16.2
properscoring==0.1          # CRPS

# --- Geo / terrain (pyproj only — rasterio now lives in requirements.txt
# since it's used at serving time too, not just training) ---
pyproj==3.6.1

# --- Optional: SFNO global-engine upgrade path (Option A) ---
# Not needed for downscaler training (the default, Option B path). Also
# genuinely fragile cross-platform: xesmf needs the ESMF C library,
# torch-harmonics compiles CUDA extensions, deepspeed doesn't support
# Windows. Install requirements-train-sfno.txt only if you're actually
# pursuing Option A -- see docs/TRAINING.md.
