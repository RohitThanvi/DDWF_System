# --- API / serving ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
python-multipart==0.0.9
orjson==3.10.7

# --- Inference ML/DL (downscaler + SIREN terrain field — the models
# DDWF actually trains/serves in the default Option B design) ---
torch==2.4.1
numpy==1.26.4
scipy==1.14.1   # slope/aspect derivation + raster resampling in terrain_fusion.py

# --- Caching / infra ---
redis==5.0.8
cachetools==5.5.0

# --- Observability ---
prometheus-fastapi-instrumentator==7.0.0
structlog==24.4.0

# --- Auth ---
pyjwt==2.9.0

# NOTE: xarray/zarr/dask/rasterio/pyproj (needed only for the optional
# SFNO global-engine upgrade path / Zarr data lake) live in
# requirements-train.txt, not here, to keep the default serving install
# lean — the default request flow never imports them.
